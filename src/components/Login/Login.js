import React from 'react';
import Header from './../Header/Header';

const Login = () => {
    return (
        <div className='container'>
            <Header></Header>
        </div>
    );
};

export default Login;