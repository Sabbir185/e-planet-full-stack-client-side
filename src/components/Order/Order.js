import React from 'react';
import Header from './../Header/Header';

const Order = () => {
    return (
        <div className='container'>
            <Header></Header>
        </div>
    );
};

export default Order;