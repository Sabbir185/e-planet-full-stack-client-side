import React from 'react';
import Header from './../Header/Header';

const About = () => {
    return (
        <div className='container'>
            <Header></Header>
        </div>
    );
};

export default About;